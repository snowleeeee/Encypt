<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="co.micol.sypertest.member.service.MemberMapper">

	<!-- <select id="memberSelectList" resultType="co.micol.sypertest.member.service.MemberVO">
		SELECT * FROM MEMBER
	</select> -->

	<select id="memberSelect" parameterType="co.micol.sypertest.member.service.MemberVO"
		resultType="co.micol.sypertest.member.service.MemberVO">
		SELECT * FROM MEMBER
		WHERE ID = #{id}
		<if test="password != null">
			AND PASSWORD = #{password}
		</if>
	</select>

	<!-- <insert id="memberInsert"></insert>

	<update id="memberUpdate"></update>
	
	<delete id="memberDelete"></delete> -->
</mapper>